<% @title "アカウント情報編集へんしゅう" %>
<h1><%= @title %></h1>

<div id='login-form'>
	<%=form_with model: @staff_number, url: staff_account_path do |f| %>
	 <%= render "form",f: f %>
	 <div class='buttons'>
		<%= f.submit "更新 こうしん Update" %>
		<%= link_to "キャンセル", staff_account_path%>
	 </div>  
	 <% end %>
</div>