<%= markup do |m| 
	p = FormPresenter.new(f, self) 
	p.with_options(required: true) do |q|
	m << q.text_field_block(:subject, "件名", size: 40, maxlength: 80)
	

	m << q.text_area_block(:body, "本文", rows: 8, maxlength: 800, style: "width: 375px; border-radius: 7px; border: 1px solid #ccc; 	color: $dark_gray;")
	end
end %>